spring:
  datasource:
    url: jdbc:h2:mem:petcare;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false
  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: localhost
    port: 1025

server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:ssl/keystore.p12
    key-store-password: changeit
    key-store-type: PKCS12

jwt:
  issuer: "petcare-api"
  secret: "P4s5FraseSuperSecretaComPeloMenos32Chars123"

springdoc:
  swagger-ui:
    oauth:
      clientId: swagger